if failing
  sleep for a a few seconds
  turn down  the rate
restart from where you left off
  continue

Ability to Restart
when starting, ask if should start at the beginning or restart
.restart file
  only the row number of the last file exists
if restart
  erase .restart file
on complete
  erase .restart file
  
# Rate limiting option 2
when recieves data updated message, 
write to a channel
  sends to a channel


